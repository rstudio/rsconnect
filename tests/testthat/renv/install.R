# renv::install("r-lib/withr")
# renv::install("cli")
# renv::install("bioc::Biobase")
# options(repos = c(
#    RLIB = 'https://r-lib.r-universe.dev',
#    CRAN = "https://cran.rstudio.com"
#  ))
# renv::install("desc")
# renv::snapshot()

library(withr)
library(cli)
library(Biobase)
library(desc)
