# Application Configuration Directory

Returns the root path used to store per user configuration data. Does
not check old locations or create the path; use `rsconnectConfigDir` for
most cases.

## Usage

``` r
applicationConfigDir()
```

## Value

A string containing the path of the configuration folder.
